[phases.setup]
nixPkgs = ["python311", "gcc", "texlive.combined.scheme-basic"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv", ". /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "python main.py"
